const prompt = require('prompt-sync')({ sigint: true });

/*
 * Let's turbo charge our original game of rock, paper, scissors by having the computer be one of the players.
 *
 * At the end of a match, the computer should ask if the player wants to play again.
 * 
 * Once they decide not to, show the total scores for both the user and computer, and indicate the winner.
 */

const main = () => {
  /**
   * This is the entry point of the exercise.
   * 
   * If you wish to implement functions, declare them outside above this function, and call them in here.
   **/
}

/* execute our code */
main();
