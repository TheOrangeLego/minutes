const prompt = require('prompt-sync')({ sigint: true });

/*
 * Play a single game of rock, paper, scissors.
 *
 * Each user inputs their option, and the program determines who has won, or if it was a tie.

 * Make sure that the user inputs are valid.
 */

const main = () => {
  /**
   * This is the entry point of the exercise.
   * 
   * If you wish to implement functions, declare them outside above this function, and call them in here.
   **/
}

/* execute our code */
main();
