const prompt = require('prompt-sync')({ sigint: true });

/*
 * Have the program think of a number between 1 to 100.
 *
 * Let the user guess, where the responses are either higher (if they guessed too low), lower (if they guess too high),
 * and correct (if they guessed correctly).
 * 
 * The game continues until the user has guessed the number correctly.
 */

const main = () => {
  /**
   * This is the entry point of the exercise.
   * 
   * If you wish to implement functions, declare them outside above this function, and call them in here.
   **/
}

/* execute our code */
main();
